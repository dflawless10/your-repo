import { Audio } from 'expo-av';

export async function playGoatSound() {
  const { sound } = await Audio.Sound.createAsync(
    require('../assets/sounds/goat.mp3') // adjust path as needed
  );
  await sound.playAsync();
}
